workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      flutter: stable
    scripts:
      - flutter packages pub get
      - flutter build ipa --release
    artifacts:
      - build/ios/ipa/*.ipa